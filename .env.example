# Секреты линии. Скопируйте в .env на сервере (в git не хранится), chmod 600 .env
# Пароли — длинные случайные строки: openssl rand -base64 32 | tr -d '/+='
POSTGRES_PASSWORD=
BOT_DB_PASSWORD=
WORKER_DB_PASSWORD=
TELEGRAM_BOT_TOKEN=
AITUNNEL_API_KEY=
LOG_LEVEL=INFO
